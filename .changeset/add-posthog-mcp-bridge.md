---
"@posthog/pi": minor
---

Add PostHog MCP bridge for pi that connects to the PostHog MCP server and dynamically registers MCP tools inside pi. Includes spill-to-file for large results, tool allowlist filtering, and configurable auth/endpoint settings.
